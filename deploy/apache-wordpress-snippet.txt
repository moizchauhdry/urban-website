# Usually NOT needed if you upload dist/ to the folder below (same as urban-app setup).
#
# Upload dist/ contents to:
#   /var/www/urbanelitelimo/connecticut-black-car-service/
#
# Live URL:
#   https://urbanelitelimo.com/connecticut-black-car-service/
#
# WordPress will serve real files before index.php when the folder exists.
# Trash any WP Page with slug "connecticut-black-car-service".
#
# Optional — only if /about refresh returns 404, add at line 1 of root .htaccess:
#
# BEGIN Urban Elite React
# <IfModule mod_rewrite.c>
# RewriteEngine On
# RewriteRule ^connecticut-black-car-service/ - [L]
# </IfModule>
# END Urban Elite React
